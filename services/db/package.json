{
  "name": "@web3socialproof/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "migrate": "yarn db:generate && yarn db:migrate",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:pull": "drizzle-kit pull",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "drizzle-kit": "^0.27.2",
    "drizzle-orm": "^0.36.0",
    "path": "^0.12.7",
    "postgres": "^3.4.4",
    "typescript": "^5"
  },
  "devDependencies": {
    "@types/node": "^20.9.0"
  }
}
